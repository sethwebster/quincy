node_modules/
dist/
build/
.env
.env.local
.claude/
.omo/
.playwright-mcp/
.sisyphus/
artifacts/
graphify-out/
.expo/
.xcode.env.local
*.keystore
*.jks
.claude/settings.local.json

# Electrobun build output
.electrobun/

# CSS build output (generated by tailwindcss CLI)
src/views/main/styles/output.css

# Convex generated — replaced by `npx convex dev`
# Keep stubs (convex/_generated/*.d.ts) in git; ignore real generated files
convex/_generated/*.js
convex/_generated/*.ts
!convex/_generated/*.d.ts

# macOS
.DS_Store

# design-sync: staged scripts, build output, caches (durable inputs under
# .design-sync/ — config.json, NOTES.md, entry.tsx, conventions.md, previews/,
# overrides/ — ARE committed)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# Local tooling state and generated artifacts (not source)
.omo/
.playwright-mcp/
.sisyphus/
artifacts/
graphify-out/

# Expo/react-native-macos port experiment — parallel shell, not part of the
# Electrobun app build. Delete or promote deliberately.
apps/QuincyDesktop/
